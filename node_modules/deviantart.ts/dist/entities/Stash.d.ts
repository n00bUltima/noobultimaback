import { DeviantArtStash, DeviantArtStashItem } from "../types/index";
export declare class Stash {
    private readonly accessToken;
    private readonly api;
    constructor(accessToken: string);
    /**
     * Fetches a stash from its stack id.
     */
    get: (params: {
        stackid: string;
        mature_content?: boolean;
    }) => Promise<DeviantArtStash>;
    /**
     * Fetches an item using its item id.
     */
    item: (params: {
        itemid: string;
        ext_submission?: boolean;
        ext_camera?: boolean;
        ext_stats?: boolean;
        mature_content?: boolean;
    }) => Promise<DeviantArtStashItem>;
}
