"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
exports.__esModule = true;
var api_1 = require("../api/api");
var RSS = /** @class */ (function () {
    function RSS(accessToken) {
        var _this = this;
        this.accessToken = accessToken;
        /**
         * Gets a deviation by searching the RSS Feed. You can provide a URL or a search query.
         */
        this.get = function (deviationURL) { return __awaiter(_this, void 0, void 0, function () {
            var deviantInfo, query_1, jsonQuery, query, json, parsed, i;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        deviantInfo = api_1["default"].parseUrl(deviationURL);
                        if (!!deviantInfo.title) return [3 /*break*/, 2];
                        query_1 = "" + deviationURL.trim().replace(/ +/g, "-");
                        return [4 /*yield*/, api_1["default"].getRSS({ q: query_1, type: "deviation", access_token: this.accessToken }, 1)];
                    case 1:
                        jsonQuery = _a.sent();
                        if (jsonQuery[0])
                            return [2 /*return*/, api_1["default"].formatJSON(JSON.stringify(jsonQuery[0]))];
                        return [2 /*return*/, Promise.reject("No results were found, try searching with another query.")];
                    case 2:
                        query = "by:" + deviantInfo.user + "+" + deviantInfo.title.trim().replace(/ +/g, "-");
                        return [4 /*yield*/, api_1["default"].getRSS({ q: query, type: "deviation", access_token: this.accessToken }, 1000)];
                    case 3:
                        json = _a.sent();
                        parsed = [];
                        for (i = 0; i < json.length; i++) {
                            parsed.push(api_1["default"].formatJSON(JSON.stringify(json[i])));
                            if (parsed[i].url.includes(deviationURL.trim())) {
                                return [2 /*return*/, parsed[i]];
                            }
                        }
                        return [2 /*return*/, Promise.reject("No results were found. The URL is invalid.")];
                }
            });
        }); };
        /**
         * Searches for deviations in the RSS feed by query. You can also specify the limit
         * and sort method (`popular` or `newest`)
         */
        this.search = function (query, limit, sort) { return __awaiter(_this, void 0, void 0, function () {
            var json, parsed, i;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        switch (sort) {
                            case "popular":
                                sort = "+sort:Apopular";
                            case "newest":
                                sort = "+sort:Atime";
                            default:
                                sort = "";
                        }
                        if (!limit)
                            limit = 50;
                        return [4 /*yield*/, api_1["default"].getRSS({ q: query + sort, type: "deviation", access_token: this.accessToken }, limit)];
                    case 1:
                        json = _a.sent();
                        parsed = [];
                        for (i = 0; i < json.length; i++) {
                            parsed.push(api_1["default"].formatJSON(JSON.stringify(json[i])));
                        }
                        if (!parsed[0])
                            return [2 /*return*/, Promise.reject("No results were found, try searching for another tag.")];
                        return [2 /*return*/, parsed];
                }
            });
        }); };
    }
    return RSS;
}());
exports.RSS = RSS;
